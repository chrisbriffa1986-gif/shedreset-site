# Board records

Internal governance records committed to the site repo as a durable git-tracked store.

This directory is NOT linked from public pages. It is intentionally reachable via direct URL only for the record; no navigation exposes it. Contents are minutes and closure evidence — not customer data.
